{
  "name": "demo-node",
  "version": "1.0.0",
  "private": true,
  "description": "Demo repo validating the org quality gate (Node).",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "echo \"no TypeScript in this demo\"",
    "test": "node --test",
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true})\""
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
